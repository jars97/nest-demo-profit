import { AlmacenesRepository } from './almacenes.repository';

describe('AlmacenesRepository', () => {
  it('should be defined', () => {
    expect(new AlmacenesRepository()).toBeDefined();
  });
});
